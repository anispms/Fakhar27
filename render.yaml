databases:
  - name: fakhar27-db
    plan: free

services:
  - type: web
    name: fakhar27-backend
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm run start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: fakhar27-db
          property: connectionString
      - key: ADMIN_EMAIL
        value: admin@example.com
      - key: ADMIN_PASSWORD
        generateValue: true
      - key: PRODUCT_IMAGE_DIRECTORY
        value: /opt/render/project/src/uploads/products
    disk:
      name: fakhar27-product-images
      mountPath: /opt/render/project/src/uploads
      sizeGB: 1

  - type: web
    name: fakhar27-frontend
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://fakhar27-backend.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
